#START_OF_IMPORT_SELECTOR
const makeSelectIs{{pascalCaseActionName}}InProgress = () => createSelector(
  select{{pascalCaseContainerName}},
  ({{camelCaseContainerName}}State) => {{camelCaseContainerName}}State.get('is{{pascalCaseActionName}}InProgress')
);

const makeSelectIs{{pascalCaseActionName}}Failed = () => createSelector(
  select{{pascalCaseContainerName}},
  ({{camelCaseContainerName}}State) => {{camelCaseContainerName}}State.get('is{{pascalCaseActionName}}Failed')
);

const makeSelect{{pascalCaseName}} = () => createSelector(
  select{{pascalCaseContainerName}},
  ({{camelCaseContainerName}}State) => {{camelCaseContainerName}}State.get('{{camelCaseName}}')
);

#END_OF_IMPORT_SELECTOR
#START_OF_EXPORT_SELECTOR
  makeSelectIs{{pascalCaseActionName}}InProgress,
  makeSelectIs{{pascalCaseActionName}}Failed,
  makeSelect{{pascalCaseName}},
#END_OF_EXPORT_SELECTOR
#END_OF_IMPORT_ALL
